using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyJumpingPoints : MonoBehaviour
{
    public Transform jumpUpPoint;
    public Transform jumpDownPoint;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Debug.Log("my jumping Up Point is at: " + jumpUpPoint.position.ToString("0.##"));
    }
}
